[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "neuralmech"
version = "0.1.0"

[tool.setuptools]
packages = ["solvers"]
py-modules = ["NN", "DL", "ML", "postprocessing"]
